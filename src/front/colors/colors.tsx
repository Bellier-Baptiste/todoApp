
const Colors = () => {

    // DARK MODE
    // Navabr
    const navbarBackgroundColor = 'rgba(66, 66, 66, 1)';
    const userLogoBackgroundColor = 'rgba(50, 50, 50, 1)';
    const inputBackgroundColor = 'white';
    const inputColor = 'black';

    // List Page
    const ticketColor = 'rgba(224, 224, 224, 0.72)';

    // LIGHT MODE
    // Navbar
    const lightNavbarBackgroundColor = 'white';

    // List Page

    return { navbarBackgroundColor, userLogoBackgroundColor, inputBackgroundColor, inputColor, 
        ticketColor,
        lightNavbarBackgroundColor};
};

export default Colors;
